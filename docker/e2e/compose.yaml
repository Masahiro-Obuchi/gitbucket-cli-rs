services:
  gitbucket:
    image: ghcr.io/gitbucket/gitbucket:4.44.0
    ports:
      - "${GB_E2E_HTTP_PORT:-18080}:8080"
    volumes:
      - "${GB_E2E_DATA_DIR:-./.tmp/e2e/gitbucket-data}:/gitbucket"
